import{useEffect,useRef}from'react';import*as THREE from'three';import{VRButton}from'three/examples/jsm/webxr/VRButton.js';
export default function WorldScene({onExit}:{onExit:()=>void}){const host=useRef<HTMLDivElement>(null);useEffect(()=>{if(!host.current)return;const root=host.current,scene=new THREE.Scene();scene.background=new THREE.Color(0x071019);scene.fog=new THREE.FogExp2(0x071019,.028);const camera=new THREE.PerspectiveCamera(68,1,.05,100);camera.position.set(0,1.7,7);const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});renderer.xr.enabled=true;renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.1;root.appendChild(renderer.domElement);const vr=VRButton.createButton(renderer);vr.className='enter-vr';root.appendChild(vr);scene.add(new THREE.HemisphereLight(0x9bdcff,0x172a19,1.3));const moon=new THREE.DirectionalLight(0x91aaff,2);moon.position.set(-7,10,4);moon.castShadow=true;scene.add(moon);const ground=new THREE.Mesh(new THREE.CircleGeometry(26,12),new THREE.MeshStandardMaterial({color:0x17291d,roughness:.95}));ground.rotation.x=-Math.PI/2;ground.receiveShadow=true;scene.add(ground);for(let i=0;i<22;i++){const a=i/22*Math.PI*2,r=13+(i%3),tree=new THREE.Group(),trunk=new THREE.Mesh(new THREE.CylinderGeometry(.2,.34,3,6),new THREE.MeshStandardMaterial({color:0x4f321d}));trunk.position.y=1.5;tree.add(trunk);for(let j=0;j<3;j++){const crown=new THREE.Mesh(new THREE.ConeGeometry(1.3-j*.17,2.5,7),new THREE.MeshStandardMaterial({color:j%2?0x1e4c35:0x285f40,roughness:.9}));crown.position.y=3+j*.82;tree.add(crown)}tree.position.set(Math.cos(a)*r,0,Math.sin(a)*r-2);scene.add(tree)}const fire=new THREE.Group();fire.position.z=-2.5;for(let i=0;i<10;i++){const a=i/10*Math.PI*2,s=new THREE.Mesh(new THREE.DodecahedronGeometry(.23),new THREE.MeshStandardMaterial({color:0x68706c,roughness:1}));s.position.set(Math.cos(a)*.82,.15,Math.sin(a)*.82);fire.add(s)}for(const r of[-.6,.6]){const log=new THREE.Mesh(new THREE.CylinderGeometry(.14,.18,1.4,7),new THREE.MeshStandardMaterial({color:0x552c17}));log.rotation.set(0,r,Math.PI/2);log.position.y=.28;fire.add(log)}const flames:THREE.Mesh[]=[];for(let i=0;i<6;i++){const c=[0xffd45b,0xff7934,0xe93620][i%3],f=new THREE.Mesh(new THREE.ConeGeometry(.27,.95,6),new THREE.MeshStandardMaterial({color:c,emissive:c,emissiveIntensity:2.5}));f.position.set((i-2.5)*.12,.75,Math.sin(i)*.12);fire.add(f);flames.push(f)}const glow=new THREE.PointLight(0xff7138,34,14,1.8);glow.position.y=1;fire.add(glow);scene.add(fire);for(const [x,z]of[[-3,-1],[3,-1],[-2,1.5],[2,1.5]] as number[][]){const seat=new THREE.Mesh(new THREE.CylinderGeometry(.26,.31,2.2,8),new THREE.MeshStandardMaterial({color:0x603b22,roughness:.9}));seat.rotation.z=Math.PI/2;seat.position.set(x,.45,z);seat.castShadow=true;scene.add(seat)}const screenFrame=new THREE.Mesh(new THREE.BoxGeometry(5.4,3.2,.2),new THREE.MeshStandardMaterial({color:0x101b28,metalness:.4}));screenFrame.position.set(0,2.4,-9);scene.add(screenFrame);const screen=new THREE.Mesh(new THREE.PlaneGeometry(5,2.8),new THREE.MeshBasicMaterial({color:0x173b5c}));screen.position.set(0,2.4,-8.88);scene.add(screen);const avatar=makeAvatar();avatar.position.set(-2,0,-1);scene.add(avatar);const keys=new Set<string>();let yaw=0,pitch=0;const kd=(e:KeyboardEvent)=>{keys.add(e.code);if(e.code==='Escape')onExit()},ku=(e:KeyboardEvent)=>keys.delete(e.code),look=(e:MouseEvent)=>{if(document.pointerLockElement===renderer.domElement){yaw-=e.movementX*.002;pitch=Math.max(-1.3,Math.min(1.3,pitch-e.movementY*.002))}},lock=()=>renderer.domElement.requestPointerLock();addEventListener('keydown',kd);addEventListener('keyup',ku);addEventListener('mousemove',look);renderer.domElement.addEventListener('click',lock);const controllers=[renderer.xr.getController(0),renderer.xr.getController(1)];controllers.forEach(c=>{scene.add(c);c.add(new THREE.Line(new THREE.BufferGeometry().setFromPoints([new THREE.Vector3(),new THREE.Vector3(0,0,-3)]),new THREE.LineBasicMaterial({color:0x73f7c5})))});const resize=()=>{renderer.setSize(root.clientWidth,root.clientHeight,false);camera.aspect=root.clientWidth/root.clientHeight;camera.updateProjectionMatrix()};const ro=new ResizeObserver(resize);ro.observe(root);resize();const clock=new THREE.Clock();renderer.setAnimationLoop(()=>{const dt=Math.min(clock.getDelta(),.05),t=clock.elapsedTime;camera.rotation.order='YXZ';camera.rotation.set(pitch,yaw,0);const f=new THREE.Vector3(-Math.sin(yaw),0,-Math.cos(yaw)),r=new THREE.Vector3(Math.cos(yaw),0,-Math.sin(yaw)),s=(keys.has('ShiftLeft')?7:3.8)*dt;if(keys.has('KeyW'))camera.position.addScaledVector(f,s);if(keys.has('KeyS'))camera.position.addScaledVector(f,-s);if(keys.has('KeyA'))camera.position.addScaledVector(r,-s);if(keys.has('KeyD'))camera.position.addScaledVector(r,s);flames.forEach((x,i)=>{x.scale.y=.82+Math.sin(t*5+i)*.16;x.rotation.y=t*(i%2?1:-1)});glow.intensity=30+Math.sin(t*7)*5;renderer.render(scene,camera)});return()=>{ro.disconnect();renderer.setAnimationLoop(null);removeEventListener('keydown',kd);removeEventListener('keyup',ku);removeEventListener('mousemove',look);renderer.dispose();root.replaceChildren()}},[onExit]);return <div className="world-scene" ref={host}/>}
function makeAvatar(){const g=new THREE.Group(),skin=new THREE.MeshStandardMaterial({color:0xd9a47e}),cloth=new THREE.MeshStandardMaterial({color:0x6b8cff});const head=new THREE.Mesh(new THREE.IcosahedronGeometry(.32,2),skin);head.position.y=1.7;const body=new THREE.Mesh(new THREE.CapsuleGeometry(.32,.68,5,10),cloth);body.position.y=1.05;g.add(head,body);return g}
